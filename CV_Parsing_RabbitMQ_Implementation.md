# 🚀 CV Parsing với RabbitMQ - Implementation Guide

> Hướng dẫn chi tiết implement tính năng CV Parsing sử dụng RabbitMQ cho SmartRecruit

---

## 📋 Tổng Quan

### Kiến Trúc Asynchronous với RabbitMQ

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │ 1. POST /api/v1/resumes/upload
       ▼
┌─────────────────────────────────────────┐
│      Java Core Service (:8080)          │
│  ┌──────────────────────────────────┐   │
│  │ 2. Upload to S3                  │   │
│  │ 3. Create Resume (status=PENDING)│   │
│  │ 4. Publish to RabbitMQ Queue     │   │
│  └──────────────────────────────────┘   │
└───────────┬─────────────────────────────┘
            │
            │ Message: {resumeId, s3Url}
            ▼
     ┌──────────────┐
     │  RabbitMQ    │
     │   Queue:     │
     │ cv-parsing   │
     └──────┬───────┘
            │
            │ 5. Consume message
            ▼
┌─────────────────────────────────────────┐
│    Python AI Service (:8000)            │
│  ┌──────────────────────────────────┐   │
│  │ 6. Download PDF from S3          │   │
│  │ 7. Extract text (PyMuPDF)        │   │
│  │ 8. AI Parse (GPT-4)              │   │
│  │ 9. Save to DB (raw data)         │   │
│  │ 10. Call Java callback           │   │
│  └──────────────────────────────────┘   │
└───────────┬─────────────────────────────┘
            │
            │ 11. POST /api/v1/resumes/{id}/parse-callback
            ▼
┌─────────────────────────────────────────┐
│      Java Core Service (:8080)          │
│  ┌──────────────────────────────────┐   │
│  │ 12. Validate & Save parsed data  │   │
│  │ 13. Update status=ACTIVE         │   │
│  │ 14. Create related entities      │   │
│  │     (skills, experiences, etc)   │   │
│  └──────────────────────────────────┘   │
└───────────┬─────────────────────────────┘
```

---

## 🔌 API Endpoints

### Java APIs

```
POST   /api/v1/resumes/upload                    - Upload CV & enqueue parsing
POST   /api/v1/resumes/{resumeId}/parse-callback - Callback từ Python (INTERNAL)
GET    /api/v1/resumes                           - List CVs
GET    /api/v1/resumes/{resumeId}                - Get CV detail
GET    /api/v1/resumes/{resumeId}/status         - Check parsing status
POST   /api/v1/resumes/{resumeId}/re-parse       - Re-parse CV
DELETE /api/v1/resumes/{resumeId}                - Delete CV
```

### Python APIs

```
(No public API - chỉ consume RabbitMQ queue)
```
